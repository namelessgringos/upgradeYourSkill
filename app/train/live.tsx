import { Text } from 'react-native-paper';
import { Screen } from '@/components/ui/Screen';

/** Placeholder so Start has somewhere to land. Replaced in Task 9. */
export default function TrainLive() {
  return (
    <Screen>
      <Text variant="headlineMedium">Session in progress</Text>
    </Screen>
  );
}
